<%= title "Članki" %>

<%= admin_section do %>
  <%= link_to "Pregled", all_articles_path %>
  <%= link_to "Dodaj članek", new_article_path %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Naziv</th>
      <th>Avtor</th>
      <th>Revija</th>
    </tr>
  </thead>
  <tbody>
    <% collection.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.authors.join(", ") %></td>
        <td><%= link_to article.issue, article.issue %></td>
      </tr>
    <% end %>
  </tbody>
</table>