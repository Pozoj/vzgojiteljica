<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <!-- Optional theme -->
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap-theme.min.css">

    <%= stylesheet_link_tag "admin" %>
    <%= javascript_include_tag "admin" %>
    <%= csrf_meta_tags %>
    <%= yield :head %>
    <title>
      <% if @page_title %>
        <%= @page_title %> (Revija Vzgojiteljica)
      <% else %>
        Vzgojiteljica - Revija za dobro prakso v vrtcih
      <% end %>
    </title>

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <header id="header" class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= admin_root_path %>">Vzgojiteljica</a>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><a href="<%= admin_entities_path %>">Naročniki</a></li>
            <li><a href="<%= admin_invoices_path %>">Računi</a></li>
            <li><a href="<%= admin_labels_path %>">Nalepke</a></li>
            <li><a href="<%= admin_bank_statements_path %>">Bančni izpiski</a></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Ostalo <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><a href="<%= admin_quantities_path %>">Količine</a></li>
                <li><a href="<%= admin_freeriders_path %>">Brezplačniki</a></li>
              </ul>
            </li>
          </ul>

          <%= form_for @filter, url: admin_entities_path, method: :get, html: {class: 'navbar-form navbar-left pull-right', role: :search} do |f| %>
            <%= f.submit style: 'visibility: hidden' %>
            <span class="text-muted">Iskanje &nbsp;</span>
            <div class="form-group">
              <%= f.search_field :global, placeholder: 'Naziv, ime, naslov, davčna, ...', class: 'form-control' %>
              <div class="advanced">
                <%= f.text_field :title, placeholder: 'Naziv', class: 'form-control' %>
                <br>
                <%= f.text_field :name, placeholder: 'Ime in priimek', class: 'form-control' %>
                <br>
                <%= f.text_field :address, placeholder: 'Naslov', class: 'form-control' %>
                <br>
                <%= f.text_field :vat_id, placeholder: 'Davčna', class: 'form-control' %>
                <br>
                <%= f.select :type, [['', ''], ['Stranka', 'Customer'], ['Naročnik', 'Subscriber']], {}, class: 'form-control' %>
              </div>
            </div>
          <% end %>
        </div>
        <!--/.nav-collapse -->
      </div>
    </header>

    <div class="container" id="container">
      <% if flash[:notice] %>
        <div id="flash" class="notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:warning] %>
        <div id="flash" class="warning"><%= flash[:warning] %></div>
      <% end %>
      <% if flash[:error] %>
        <div id="flash" class="error"><%= flash[:error] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div id="flash" class="alert"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>

    <!-- Latest compiled and minified JavaScript -->
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  </body>
</html>
