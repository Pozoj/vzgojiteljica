<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <!-- Optional theme -->
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap-theme.min.css">

    <%= stylesheet_link_tag "admin" %>
    <%= javascript_include_tag "admin" %>
    <%= csrf_meta_tags %>
    <%= yield :head %>
    <title>
      <% if @page_title %>
        <%= @page_title %> (Vzgojiteljica)
      <% else %>
        Vzgojiteljica
      <% end %>
    </title>

    <style type="text/css">
      #container {
        padding-bottom: 150px;
      }
      footer {
        position: fixed;
        bottom: 0px;
        left: 0px;
        width: 100%;
        height: 100px;
        background: #f3f3f3;
        border-top: #999;
      }
      footer .pull-left {
        padding-top: 20px;
      }
      footer h1 {
        margin: 0;
        padding: 0;
        line-height: 100px;
        color: #ddd;
      }
      footer
    </style>

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body id="<%= body_id %>" class="<%= body_class %>">
    <div class="container" id="container">
      <% if flash[:notice] %>
        <div id="flash" class="alert alert-info"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:warning] %>
        <div id="flash" class="alert alert-warning"><%= flash[:warning] %></div>
      <% end %>
      <% if flash[:error] %>
        <div id="flash" class="alert alert-danger"><%= flash[:error] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div id="flash" class="alert alert-warning"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>

    <footer>
      <div class="container">
        <div class="pull-left">
          <strong>POZOJ, Darja Slapničar s.p.</strong>
          <br>
          Cesta talcev 12, 3320 Velenje
          <br>
          E-pošta: <%= mail_to "info@pozoj.si" %>
        </div>
        <h1 class="pull-right">Revija Vzgojiteljica</h1>
      </div>
    </footer>

    <!-- Latest compiled and minified JavaScript -->
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
  </body>
</html>
